spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=create-drop

# To see the full stack trace of Maven errors, run:
# mvn clean test -e

# To enable Maven debug logs, run:
# mvn clean test -X

# If you still see ApplicationContext errors, check for:
# 1. Only one @SpringBootApplication or @SpringBootConfiguration class in your backend.
# 2. No conflicting beans or configuration classes.
# 3. Correct test configuration (e.g., @SpringBootTest(classes = ClrBackendApplication.class)).
# 4. Proper use of @MockBean for dependencies in your tests.